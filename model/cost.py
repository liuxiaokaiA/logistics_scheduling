import logging


log = logging.getLogger('default')


VALUE_MAX = 10000000

order_data = {
    order_id: {
        'is_loading': 0
        'object': order_object
    }
}

def get_order_data():
    global order_data

def compute_cost(gene):
    gene_data = gene.gene_data

    pass
